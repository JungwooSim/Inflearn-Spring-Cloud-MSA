server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file:///Users/bigpenguin/project/Cloud-config
          search-paths: /Inflearn-Spring-Cloud_MSA/local-repo
