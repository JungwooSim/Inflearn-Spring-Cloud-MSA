server:
  port: 8888

spring:
  application:
    name: config-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  profiles:
    active: native
  cloud:
    config:
      server:
        # native file access
        native:
          search-locations: file://${user.home}/project/Inflearn-Spring-Cloud-MSA/native-file-repo
        git:
#          # git file access
          uri: file:///Users/bigpenguin/project/Cloud-config
          search-paths: /Inflearn-Spring-Cloud_MSA/local-repo
          # git access
#          uri: https://github.com/JungwooSim/Cloud-config
#          username: # private
#          password: # private

management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh
